expr: KEYWORD IDENTIFIER EQ expr
      term ((PLUS|MINUS) term)*
      term POWER term

term: fact ((MUL|DIV) fact)*
      fact POWER fact

factor: INT|FLOAT
        (PLUS|MINUS) factor
        LPAREN expr RPAREN
        IDENTIFIER