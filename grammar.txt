

expr: KEYWORD IDENTIFIER EQ expr
      IDENTIFIER EQ expr

for: for (var_assign; cond_expr; arithmetic_expr):
         expr
      
if_else: if (cond_expr):
            expr
         else if (sond_expr):
            expr
         else:
            expr

cond_expr: comp_expr ((AND|OR) comp_expr)
      
comp_expr: NOT comp_expr
           arithmetic_expr ((COMPARISON_OP) arithmetic_expr)

arithmetic_expr: term ((PLUS|MINUS|BITWISE_OP) term)*
                 term POWER term

term: fact ((MUL|DIV) fact)*
      fact POWER fact

factor: INT|FLOAT
        (PLUS|MINUS|BIT_NOT) factor
        LPAREN expr RPAREN
        IDENTIFIER